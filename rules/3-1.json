{
  "contentType": "application/vnd.gorules.decision",
  "nodes": [
    {
      "id": "input_1",
      "type": "inputNode",
      "name": "Request",
      "position": { "x": 100, "y": 200 }
    },
    {
      "id": "fn_interval",
      "type": "functionNode",
      "name": "Interval (minutes)",
      "position": { "x": 420, "y": 200 },
      "content": "/*\n * How long an OLT alarm must persist before we consider it 'sustained'\n * and proceed to traffic-trend analysis.\n * Operators tune this without redeploying the pipeline.\n */\nexport const handler = async (input) => ({ interval: 3 });\n"
    },
    {
      "id": "output_1",
      "type": "outputNode",
      "name": "Response",
      "position": { "x": 740, "y": 200 }
    }
  ],
  "edges": [
    { "id": "e1", "type": "edge", "sourceId": "input_1",     "targetId": "fn_interval" },
    { "id": "e2", "type": "edge", "sourceId": "fn_interval", "targetId": "output_1" }
  ]
}
