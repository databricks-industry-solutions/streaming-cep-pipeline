# Exclude node_modules and other large directories from deployment
frontend/node_modules/
frontend/.git/
__pycache__/
.pytest_cache/
rules_mock/
.git/
.DS_Store

# The frontend `dist/` is .gitignore'd (build artifact) but MUST be included
# in bundle uploads — the FastAPI backend serves it via StaticFiles.
!frontend/dist/
!frontend/dist/**
